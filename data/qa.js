const QA_BANK = [
  {
    category: "Python & Systems",
    items: [
      { q: "What is the GIL and why does it matter for RL training code?", a: "The Global Interpreter Lock allows only one thread to execute Python bytecode at a time, so CPU-bound parallelism needs multiple processes, not threads. I/O-bound work (disk, network) can still benefit from threads since I/O releases the GIL." },
      { q: "When would you use multiprocessing versus threading versus asyncio?", a: "Multiprocessing for CPU-bound parallel work (data preprocessing, multiple simulator instances). Threading for I/O-bound work sharing memory cheaply. asyncio for high-concurrency I/O without the overhead of OS threads." },
      { q: "What is the difference between a shallow copy and a deep copy?", a: "A shallow copy duplicates the outer container but keeps references to the same nested objects; a deep copy recursively duplicates everything. Mutating a nested list in a shallow copy affects the original." },
      { q: "Why can mutable default arguments in Python be a bug?", a: "A mutable default is created once at function definition time and shared across all calls that do not override it, so mutations persist across unrelated calls. Use None as the default and create the mutable object inside the function body." },
      { q: "What is a generator, and when would you prefer it over building a full list?", a: "A generator produces values lazily, one at a time, without materializing the full sequence in memory — important when streaming large trajectory datasets that do not fit in memory at once." }
    ]
  },
  {
    category: "NumPy & PyTorch",
    items: [
      { q: "What is broadcasting and what are its rules?", a: "Broadcasting lets operations work on arrays of different shapes by implicitly expanding dimensions of size 1 to match, comparing shapes from the trailing dimension; dimensions must be equal or one of them must be 1." },
      { q: "How does PyTorch autograd actually work?", a: "Every differentiable operation builds a node in a dynamic computation graph during the forward pass; calling backward() traverses this graph in reverse topological order, applying the chain rule to accumulate gradients into .grad." },
      { q: "Why call optimizer.zero_grad() before each backward pass?", a: "PyTorch accumulates gradients into .grad by default rather than overwriting them — useful for gradient accumulation across microbatches, but wrong for a standard step unless explicitly zeroed first." },
      { q: "Why might you use .detach() in an RL loss function?", a: "To stop gradients from flowing through a term that should be treated as a fixed target or constant, such as a baseline or a target network's output." },
      { q: "What causes a shape mismatch error and how do you debug it fast?", a: "Print .shape at each intermediate step to localize exactly which operation produced an unexpected shape, rather than guessing from the final error line." }
    ]
  },
  {
    category: "Complexity & Algorithms",
    items: [
      { q: "When is a hash map lookup not actually O(1)?", a: "Worst case is O(n) if many keys collide into the same bucket; Python's dict mitigates this with a good hash function and resizing, but the caveat is worth naming rather than stating O(1) unconditionally." },
      { q: "Why is quicksort's worst case O(n^2) despite average O(n log n)?", a: "A poor pivot choice (already-sorted input with a naive first-element pivot) can produce maximally unbalanced partitions at every level; randomized or median-of-three pivot selection avoids this in practice." },
      { q: "When would you choose a heap over sorting the full array?", a: "When you only need the top-k elements repeatedly or as a stream arrives, a size-k heap is O(n log k) versus O(n log n) for a full sort, and works incrementally on streaming data." },
      { q: "What is amortized complexity, with an example?", a: "The average cost per operation over a worst-case sequence of operations, even if individual operations occasionally cost more. Dynamic array append is O(1) amortized despite occasional O(n) resizes." }
    ]
  },
  {
    category: "RL Concepts, Quick Facts",
    items: [
      { q: "On-policy versus off-policy, in one line each?", a: "On-policy learns about the policy currently being used to act (PPO, A2C); off-policy can learn from data generated by a different or past policy (Q-learning, DDPG, TD3, SAC), usually more sample-efficient but less stable." },
      { q: "Why does DQN need a target network?", a: "Bootstrapping the TD target from the same rapidly-changing network used to select actions creates a moving target and destabilizes training; a periodically-updated, frozen target network breaks that feedback loop." },
      { q: "What does the discount factor gamma actually control?", a: "How much the agent weights future reward against immediate reward; gamma close to 1 optimizes for long-horizon return, gamma close to 0 makes the agent myopic. It also affects the effective planning horizon and variance of return estimates." },
      { q: "Why is exploration harder in continuous action spaces than discrete?", a: "You cannot enumerate all actions to be epsilon-greedy over; continuous control typically uses action noise (Gaussian or Ornstein-Uhlenbeck) or entropy-regularized stochastic policies (SAC) instead." },
      { q: "What is the credit assignment problem?", a: "Determining which of many past actions in a long trajectory were actually responsible for a later reward; GAE, eligibility traces, and reward shaping all partly address this." }
    ]
  },
  {
    category: "ML System Design Vocabulary",
    items: [
      { q: "Data parallel versus model parallel training?", a: "Data parallel replicates the full model on each device and splits the batch, synchronizing gradients; model parallel splits the model itself across devices when it does not fit on one, at the cost of more complex communication." },
      { q: "What does FSDP add over standard data-parallel training?", a: "Fully Sharded Data Parallel shards parameters, gradients, and optimizer state across devices instead of replicating them fully, trading extra communication for much lower per-device memory use." },
      { q: "What is a canary deployment?", a: "Rolling out a change to a small subset of traffic or a fleet first, monitoring key metrics, and only proceeding to full rollout if the canary group's metrics stay within bounds." },
      { q: "Why does inference optimization matter for this role specifically?", a: "A policy has to run within a fixed real-time control cycle on embedded or constrained onboard hardware; a numerically accurate but too-slow model is not deployable regardless of task performance." }
    ]
  }
];
